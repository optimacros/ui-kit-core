import { default as React } from 'react';

type Props = {
    error?: React.ReactNode;
    label: string;
    className?: string;
    classNameContainer?: string;
};
export declare class TextArea extends React.PureComponent<Props> {
    render(): React.JSX.Element;
}
export {};
