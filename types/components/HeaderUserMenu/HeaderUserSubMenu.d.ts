import { default as React } from 'react';

type Props = {
    label: string;
    onClick: () => void;
    className?: string;
};
export declare const HeaderUserSubMenu: (props: React.PropsWithChildren<Props>) => React.JSX.Element;
export {};
