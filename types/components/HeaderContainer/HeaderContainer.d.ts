import { default as React, Component } from 'react';

type Props = {
    className: string;
};
export declare class HeaderContainer extends Component<React.PropsWithChildren<Props>> {
    render(): React.JSX.Element;
}
export {};
